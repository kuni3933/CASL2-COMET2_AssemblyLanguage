<html>
<head>
<title>WCASL Ⅱ　シミュレータの使い方</title>
<link href="../../wcasl2-applet/doc/wstyle.css" rel="stylesheet" type="text/css">
<meta http-equiv="Content-Type" content="text/html; charset=shift_jis"></head>
<body>
<h1>WCASLⅡシミュレータ</h1>
<p></p>
<h2>WCASL-Ⅱ シミュレータの概要</h2>
<p>以下のようなCASLモードとCOMETモードの2つがあります．これらのモードは任意の時点で切り替えることができます．
これらのウィンドウの見方はWindows版WCASLⅡと同じですので，WCASLⅡホームページの説明を参照してください．</p>
<ul>
<li>CASLモード：CASLⅡプログラミング習得のためのモード (<a href="http://www.ics.teikyo-u.ac.jp/wcasl2/usage/caslsim.html#caslSimWindow">WCASLⅡホームページ：見方の説明</a>)</li>
<li>COMETモード：CPUとしてのCOMETⅡの動作を理解するためのモード(<a href="http://www.ics.teikyo-u.ac.jp/wcasl2/tutorial/lesson01/comet02.html">WCASLⅡホームページ：見方の説明</a>)</li>
</ul>

<p>どちらのモードでも，Enterキーを押すことでステップが一つずつ進みます．
また，BackSpaceキーを押すことでステップが一つ戻ります．</p>

<p>以降では，コマンドをメニュー項目ごとに説明します．</p>

<h2 id="menu_view">表示</h2>  <p></p>   
<h3>スタック・ディスプレイ</h3>
<p>スタックとディスプレイのエリアを表示します．メニューの名称にチェックが付くと表示されている状態です．
チェックが付いているときに，このコマンドを選択するとスタックとディスプレイのエリアが非表示になります．</p>

<h3>符号無表示</h3>
<p>CASLモードシミュレーションで，レジスタエリアとラベルエリアの10進数表示を符号付きで表示するか，符号無しで表示するかの設定をします．
CASLモードのときのみ有効なコマンドです．</p>
<p>メニューの名称にチェックが付いているときは符号無し表示です．このときにこのコマンドを選択すると，符号付き表示の戻ります．</p>

<h3>表示モード変更</h3>
<p>CASLモードとCOMETモードを切り替えます．このコマンドを選択するたびに，切り替えが行われます．</p>
<h2 id="menu_command">コマンド</h2> <p></p>
<h3>ロードモード</h3>
<p>ここにマウスカーソルを持って行くと，以下の三つのロードモードから一つが選択できます．</p>
<ul>
<li>0で初期化</li>
<li>-1で初期化</li>
<li>初期化無し</li>
</ul>
<p>これは，プログラムをロードするときに，レジスタやメモリエリアをどのような値で初期化するかを指定するものです．
デフォールトでは-1(#FFFF)になっています．</p>

<h3>先頭アドレス設定</h3>

<p>CASLⅡプログラムをロードする領域の先頭アドレスを設定します．0020，0030，0040，0050，0100から選択できます．</p>

<h3>リロード</h3>

<p>CASLⅡプログラムをロードし直し，実行前の状態に初期化します．</p>

<h3>1ステップ実行</h3>

<p>1ステップ実行します．Enterキー，または，下矢印キーでも同じです．</p>

<h3>1ステップ戻る</h3>

<p>1ステップ戻ります．BackSpaceキー，または，上矢印キーでも同じです．</p>
<h3>最後まで実行</h3>
<p>プログラムを一気に最後まで実行します．</p>
<h2 id="menu_version">バージョン情報</h2>   <p></p>

<h3>バージョン</h3>
<p>バージョン情報を表示します．</p>

<hr>
WCASL-Ⅱ ホームページ  <a href="http://www.ics.teikyo-u.ac.jp/wcasl2/">http://www.ics.teikyo-u.ac.jp/wcasl2/</a>
</body>
</html>