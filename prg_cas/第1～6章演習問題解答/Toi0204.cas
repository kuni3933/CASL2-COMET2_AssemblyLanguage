TOI0204 START
        RPUSH
        LAD     GR1,DATA        ;DATAのアドレスをGR1へ格納
        LD      GR3,0,GR1       ;DATA+0番地の値を読み込む
        LD      GR4,1,GR1       ;DATA+1番地の値を読み込む
        LD      GR5,2,GR1       ;DATA+2番地の値を読み込む
        LD      GR6,3,GR1       ;DATA+3番地の値を読み込む
        LD      GR7,3,GR1       ;DATA+4番地の値を読み込む
        RPOP
        RET
DATA    DC      'CASL2'
        END
